ext {

    config = [

            //sdk config
            compileSdkVersion     : 30,
            buildToolsVersion     : "29.0.2",
            minSdkVersion         : 23,
            targetSdkVersion      : 29,
            versionCode           : 1,
            versionName           : "1.0.0",

            kotlinVersion         : "1.5.21",

    ]
}